[phases.setup]
nixPkgs = ["dotnet-sdk_9"]

[phases.build]
cmds = [
  "dotnet restore MyPortforlioWebsite.csproj -force",
  "dotnet publish MyPortforlioWebsite.csproj -c Release -o out"
]

[start]
cmd = "dotnet out/MyPortforlioWebsite.dll"